import React from 'react';
import classes from './reusableCodeCmp.module.css';
import Text from '../../../utils/TextSection';

function ReusableCode() {
  return <Text h1={'Unavailable right now!'} />;
}

export default ReusableCode;
